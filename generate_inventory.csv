import pandas as pd
import random
from faker import Faker

fake = Faker()

inventory = []

for i in range(1, 501):  # assuming 500 products
    inventory.append({
        "product_id": i,
        "stock_level": random.randint(0, 100),
        "warehouse_location": fake.city()
    })

df = pd.DataFrame(inventory)
df.to_csv("../data/inventory.csv", index=False)

print("✅ inventory.csv generated successfully!")