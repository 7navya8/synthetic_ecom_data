import pandas as pd
import random
from faker import Faker

fake = Faker()

orders = []

for i in range(1, 2001):
    orders.append({
        "order_id": i,
        "customer_id": random.randint(1, 1000),  # assuming 1000 customers
        "product_id": random.randint(1, 500),    # assuming 500 products
        "quantity": random.randint(1, 5),
        "order_date": fake.date_between(start_date='-90d', end_date='today')
    })

df = pd.DataFrame(orders)
df.to_csv("../data/orders.csv", index=False)

print("✅ orders.csv generated successfully!")